"""LLM 总结用的 prompt 模板。

定位：「带引用的笔记整理员」，不是「写读后感」。
读者目标是不看视频就能掌握全部有用信息——包括 UP 主原话、举例、数据。
所以这里**不设字数上限**，强制保留原文金句和具体内容。
"""

# 单段总结（短视频走这条；长视频每个分段也走这条）。
# 输出 JSON 形态，外层 summary 字段是 Markdown 笔记。
SUMMARY_PROMPT = """你是视频字幕的「笔记整理员」，不是「摘要作者」。读者的目标是：不看视频就能掌握全部有用信息，包括 UP 主原话、举的例子、说的数据。

# 输入
- 视频标题：{{title}}
- 字幕来源：{{subtitle_source}}
- 字幕内容：
{{transcript}}

# 总原则
- 字幕里**说过**的话尽量保留；不是「值得保留才保留」，而是「除非是纯口水话否则就保留」。
- 宁可让笔记长，不要丢信息。长度跟视频长度走，**不设上限**。
- 输出是给读者的笔记，不是给视频做广告。**不**写「精彩」「值得一看」「让人深思」这类评价。
- 输出语言：{{language}}。

# 处理细节
1. 口头禅（「那个」「就是说」「对吧」「嗯」）可省。
2. 重复表达留一次。
3. 错字、ASR 误识可按上下文修正，**但不要替换原词**——书名、工具名、人名以 UP 主说的为准，ASR 拼错也尽量按上下文还原。
4. 字幕里没有的内容**不要补**。不确定就不写。
5. 字幕来源是 ASR 自动识别时，对句界、对话切换更谨慎；不确定的内容不要当事实。

# 输出格式
只输出一个合法 JSON 对象：{"summary":"..."}。
- 不要 markdown 代码块、不要 JSON 之外的解释文字。
- summary 字段是 Markdown 笔记，按下面的结构组织。

# summary 字段的结构

## TL;DR
一句话（30 字内）：这个视频在讲什么。不要照抄标题。

## 核心要点
按视频时间顺序列 5-15 条要点，每条 1-3 句话。**短视频可以少，长视频不要少**。
每条要点应该回答「视频在这一段讲了什么具体内容」，不要泛泛写「介绍了 X 的优势」，要写「X 比 Y 快 3 倍，因为 Z」。

## 原文金句
从字幕里挑 5-15 句**值得直接引用**的话，用 `> ` 引用块按出场顺序列出。
判断标准：观点强烈、定义清晰、有具体数据、有反常识结论、或者有梗。
**不要**复述、不要改写——逐字引用（修正明显错字除外）。
如果视频实在没有值得引用的句子（纯流水账 vlog），写「（无突出原话）」。

## 提到的具体内容
逐项列出视频里出现的「专有名词」，**有就列，没有就省略对应小项**：
- **人物**：提到的人名（含简短背景，如「李飞飞（ImageNet 作者）」）
- **作品/工具**：书名、论文、软件、网站、产品、API
- **数据/数字**：百分比、价格、参数量、时间、距离等具体数字
- **案例**：UP 主举的具体例子（公司、事件、新闻）
- **链接/资源**：UP 主口播或字幕里提到的 URL、GitHub repo、邮箱

每项 1 行，**全部列出，不设上限**。

## 结论 / 行动建议
视频末尾的明确结论、推荐、号召或操作步骤。没有就省略本节。

## 字幕质量备注
仅当字幕明显残缺、ASR 噪声大、或某段听不清时写一句话说明；正常就省略本节。
"""


# JSON 解析失败时的修复 prompt：把模型瞎写的输出强行套回 {"summary":"..."} 结构。
# 这里不能改，逻辑上是"承接已有内容"。
SUMMARY_REPAIR_PROMPT = """You will receive a model output that should represent a JSON object with a single field named "summary".

Your task:
1. Preserve the original meaning.
2. Convert the content into valid JSON.
3. Output only the JSON object, with exactly this shape:
{"summary":"..."}
4. Do not add markdown fences, explanations, or extra fields.

If the original output already contains a useful summary body, keep it inside "summary" as markdown text."""


# 流式总结主路径：直接输出 Markdown，不走 JSON。
# 内容规则和 SUMMARY_PROMPT 一致，只换输出格式约束。
SUMMARY_MARKDOWN_PROMPT = """你是视频字幕的「笔记整理员」，不是「摘要作者」。读者的目标是：不看视频就能掌握全部有用信息，包括 UP 主原话、举的例子、说的数据。

# 输入
- 视频标题：{{title}}
- 字幕来源：{{subtitle_source}}
- 字幕内容：
{{transcript}}

# 总原则
- 字幕里**说过**的话尽量保留；不是「值得保留才保留」，而是「除非是纯口水话否则就保留」。
- 宁可让笔记长，不要丢信息。长度跟视频长度走，**不设上限**。
- 输出是给读者的笔记，不是给视频做广告。**不**写「精彩」「值得一看」「让人深思」这类评价。
- 输出语言：{{language}}。

# 处理细节
1. 口头禅（「那个」「就是说」「对吧」「嗯」）可省。
2. 重复表达留一次。
3. 错字、ASR 误识可按上下文修正，**但不要替换原词**——书名、工具名、人名以 UP 主说的为准，ASR 拼错也尽量按上下文还原。
4. 字幕里没有的内容**不要补**。不确定就不写。
5. 字幕来源是 ASR 自动识别时，对句界、对话切换更谨慎；不确定的内容不要当事实。

# 输出格式
直接输出 Markdown，不要 JSON、不要代码块、不要解释文字。按下面的结构组织。

## TL;DR
一句话（30 字内）：这个视频在讲什么。不要照抄标题。

## 核心要点
按视频时间顺序列 5-15 条要点，每条 1-3 句话。**短视频可以少，长视频不要少**。
每条要点应该回答「视频在这一段讲了什么具体内容」，不要泛泛写「介绍了 X 的优势」，要写「X 比 Y 快 3 倍，因为 Z」。

## 原文金句
从字幕里挑 5-15 句**值得直接引用**的话，用 `> ` 引用块按出场顺序列出。
判断标准：观点强烈、定义清晰、有具体数据、有反常识结论、或者有梗。
**不要**复述、不要改写——逐字引用（修正明显错字除外）。
如果视频实在没有值得引用的句子（纯流水账 vlog），写「（无突出原话）」。

## 提到的具体内容
逐项列出视频里出现的「专有名词」，**有就列，没有就省略对应小项**：
- **人物**：提到的人名（含简短背景，如「李飞飞（ImageNet 作者）」）
- **作品/工具**：书名、论文、软件、网站、产品、API
- **数据/数字**：百分比、价格、参数量、时间、距离等具体数字
- **案例**：UP 主举的具体例子（公司、事件、新闻）
- **链接/资源**：UP 主口播或字幕里提到的 URL、GitHub repo、邮箱

每项 1 行，**全部列出，不设上限**。

## 结论 / 行动建议
视频末尾的明确结论、推荐、号召或操作步骤。没有就省略本节。

## 字幕质量备注
仅当字幕明显残缺、ASR 噪声大、或某段听不清时写一句话说明；正常就省略本节。
"""


# 长视频分段总结后的 merge prompt（JSON 输出）。
# 关键约束：原文金句和具体内容**不允许**在 merge 阶段被删减——这是上一版最大的损失点。
SUMMARY_MERGE_PROMPT = """你是视频笔记的编辑。请基于用户提供的视频标题和分段笔记，合并成一份完整笔记。

# 原则
- 只基于分段笔记内容，不引入外部知识、猜测或个人观点。
- 输出语言必须为：{{language}}。

# 合并规则
- 「TL;DR」「核心要点」可以合并、精简、去重。
- 「原文金句」**不允许删减**。两段引用同一句只保留一次；其余全部按出场顺序保留。
- 「提到的具体内容」**不允许删减**。不要把多个分段的具体名字、数字合并成抽象描述。
- 「结论 / 行动建议」按视频结尾段为准；前几段的临时性结论可省。
- 「字幕质量备注」若有任一分段标记，合并保留。

# 输出格式
只输出一个合法 JSON 对象：{"summary":"..."}。
- 不要 markdown 代码块、不要 JSON 之外的解释文字。
- summary 字段是 Markdown 笔记，结构同分段笔记（TL;DR / 核心要点 / 原文金句 / 提到的具体内容 / 结论 / 字幕质量备注）。

视频标题：
{{title}}

分段笔记：
{{transcript}}
"""


# 长视频分段 merge 的 Markdown 流式版本。内容规则同上。
SUMMARY_MERGE_MARKDOWN_PROMPT = """你是视频笔记的编辑。请基于用户提供的视频标题和分段笔记，合并成一份完整笔记。

# 原则
- 只基于分段笔记内容，不引入外部知识、猜测或个人观点。
- 输出语言必须为：{{language}}。

# 合并规则
- 「TL;DR」「核心要点」可以合并、精简、去重。
- 「原文金句」**不允许删减**。两段引用同一句只保留一次；其余全部按出场顺序保留。
- 「提到的具体内容」**不允许删减**。不要把多个分段的具体名字、数字合并成抽象描述。
- 「结论 / 行动建议」按视频结尾段为准；前几段的临时性结论可省。
- 「字幕质量备注」若有任一分段标记，合并保留。

# 输出格式
直接输出 Markdown，不要 JSON、不要代码块、不要解释文字。
结构同分段笔记：TL;DR / 核心要点 / 原文金句 / 提到的具体内容 / 结论 / 行动建议 / 字幕质量备注。

视频标题：
{{title}}

分段笔记：
{{transcript}}
"""
