# 后端 API 地址；本地开发不动即可
VITE_API_BASE_URL=http://localhost:17821

# 后端 API_TOKEN 留空 → 这里也留空
# 注意：VITE_API_TOKEN 会在 build 时打进 JS bundle，任何访问页面的人都能在
# devtools 里看到，公网部署请叠一层反向代理鉴权（Vercel Protection / Cloudflare Access）
VITE_API_TOKEN=
